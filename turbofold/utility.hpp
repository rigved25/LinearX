#ifndef TF_UTILITIES_HPP
#define TF_UTILITIES_HPP

#include <unordered_map>
#include <tuple>

enum VerboseState {
    SILENT = 0,
    VERBOSE = 1,
    DEBUG = 2,
    DETAIL = 3
};

#endif // TF_UTILITIES_HPP
